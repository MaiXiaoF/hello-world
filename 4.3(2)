大小写转换
int main()
{
	char ch = 0;
	while ((ch = getchar()) != EOF)
	{
		if (ch >= 'a' && ch <= 'z') 
		{
			putchar(ch - 32);
			printf("\n");
		}
		else if (ch >= 'A' && ch <= 'Z')
		{
			putchar(ch + 32);
			printf("\n");
		}
		getchar();
	}

	return 0;
}
